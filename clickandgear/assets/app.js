// assets/app.js

import 'bootstrap'; // Inclure Bootstrap
import './styles/app.scss'; // Inclure les styles

// Optionnel : Inclure jQuery si tu en as besoin pour certains composants Bootstrap
const $ = require('jquery');
global.$ = global.jQuery = $;
